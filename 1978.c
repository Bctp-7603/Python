// 소수 구하기

#include <stdio.h>

int main() {
    int count;
    int result = 0;

    scanf("%d", &count);

    int arr[count];

    for(int i = 0; i < count; i++) {
        scanf("%d", &arr[i]);
    }

    // 소수 구하다 침대에서 뒹굴뒹굴 거리기

    printf("%d\n", result);

    return 0;
}