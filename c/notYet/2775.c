/*
#include <stdio.h>

int main() {
    int t;
    
    scanf("%d", &t);

    int k[t], n[t], count[t];

    for(int i = 0; i < t; i++) {
        scanf("%d", &k[i]);
        scanf("%d", &n[i]);
    }

    for(int i = 0; i < t; i++) {
        if(k[i] == 0) {
            n[i] = i + 1;
        }
        else {
            if(n[i])
        }
    }

    return 0;
}
*/